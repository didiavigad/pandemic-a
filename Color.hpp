#pragma once
#include <vector>
#include <string>

using namespace std;

namespace pandemic{
    enum Color{
        Black,
        Blue,
        Red,
        Yellow
    };

    static const vector<string> colors = {"Black","Blue","Red","Yellow"};
};
